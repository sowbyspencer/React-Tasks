{
  "name": "react-dev",
  "version": "0.0.0",
  "scripts": {
    "prebuild": "echo 'Building the project...'",
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject"
  },
  "dependencies": {
    "react": "^18.0.0",
    "react-dev": "file:",
    "react-dom": "^18.0.0",
    "react-scripts": "^5.0.0",
    "uuid": "^10.0.0"
  },
  "engines": {
    "node": ">=14.x"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
